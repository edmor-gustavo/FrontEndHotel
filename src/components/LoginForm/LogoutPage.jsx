// eslint-disable-next-line no-unused-vars
import React from "react";

const LogoutPage = () => {
    // Lógica para fazer logout (por exemplo, limpar o token de autenticação)
    return (
        <div>
            <h2>Logout</h2>
            <p>Logout realizado com sucesso.</p>
        </div>
    );
};

export default LogoutPage;
